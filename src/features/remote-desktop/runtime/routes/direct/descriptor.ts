import type { RemoteDesktopRouteDescriptor } from '../../core/routeRegistry.ts'

export const directRouteDescriptor: RemoteDesktopRouteDescriptor = Object.freeze({
  id: 'direct',
  configVersion: 1,
})
