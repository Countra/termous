import type { RemoteDesktopRouteDescriptor } from '../../core/routeRegistry.ts'

export const sshTunnelRouteDescriptor: RemoteDesktopRouteDescriptor = Object.freeze({
  id: 'ssh_tunnel',
  configVersion: 1,
})
